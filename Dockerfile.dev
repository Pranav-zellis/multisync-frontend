# 1. Use a lightweight Node image
FROM node:18-alpine

# 2. Set working directory
WORKDIR /usr/src/app

# 3. Copy package files first (for layering/caching)
COPY package*.json ./

# 4. Install dependencies
RUN npm install

# 5. Copy everything else (including next.config.js and src/)
COPY . .

# 6. Expose Next.js default port
EXPOSE 3000

# 7. Default command (Compose will override with `npm run dev`)
CMD ["npm", "run", "dev"]
